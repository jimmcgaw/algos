# Algorithms

Back to basics in Python

I'll often implement a data structure or function in here for use in a Leetcode exercise, before
taking it to their godawful in-browser editor.

## Running Tests

Requires `pytest` library:

```
pip install pytest
```

To run:

```
make test
```